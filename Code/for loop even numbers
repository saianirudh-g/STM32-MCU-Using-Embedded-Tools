#include <stdio.h>
#include <stdint.h>

int main(void)
{
    int32_t num1, num2;
    uint32_t even = 0;

    printf("Enter the first number: ");
    scanf("%d", &num1);

    printf("Enter the last number: ");
    scanf("%d", &num2);

    printf("Even numbers are:\n");
#include <stdio.h>
#include <stdint.h>

int main(void)
{
    int32_t num1, num2;
    uint32_t even = 0;

    printf("Enter the first number: ");
    scanf("%d", &num1);

    printf("Enter the last number: ");
    scanf("%d", &num2);

    printf("Even numbers are:\n");
    while (num1 <= num2) {
        if (!(num1 % 2)) {
            printf("%4d\t", num1);
            even++;
        }
    }

    printf("\nTotal even numbers: %u\n", even);

    // Clear buffer and wait for key
    int c;
    while ((c = getchar()) != '\n' && c != EOF) { }
    printf("Press any key to exit...\n");
    getchar();

    return 0;
}


    printf("\nTotal even numbers: %u\n", even);

    // Clear buffer and wait for key
    int c;
    while ((c = getchar()) != '\n' && c != EOF) { }
    printf("Press any key to exit...\n");
    getchar();

    return 0;
}

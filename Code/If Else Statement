#include <stdio.h>

int main(void)
{
    int age = 0;
    printf("Enter you age here :");
    scanf("%d",&age);

    if(age < 18){
    	printf("Sorry!! You are not eligible to Vote\n");
    }
    if(age >= 18){
    	printf("Congratulations!! You are now eligible to Vote\n");
    }

    printf("Press Enter key to exit this application\n");
    while(getchar() != '\n'){
    	// just read the input
    }
    getchar();
}
